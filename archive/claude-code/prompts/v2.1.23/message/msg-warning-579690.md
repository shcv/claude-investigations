---
id: msg-warning-579690
name: Warning Message
category: message
subcategory: warning
source_line: 579690
---

,
            );
        }
        if ((dnA(U6?.agentType), OA && U6 && !t && !UD(U6))) {
          let T6 = U6.getSystemPrompt();
          if (T6) t = T6;
        }
        if (
          b8() &&
          MA?.agentId &&
          MA?.agentName &&
          MA?.teamName &&
          MA?.agentType
        ) {
          let T6 = T1.activeAgents.find((W7) => W7.agentType === MA.agentType);
          if (T6) {
            let W7;
            if (T6.source === "built-in")
              h(
                
